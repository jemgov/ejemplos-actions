name: Variable de ambiente

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
      
      - name: Configura variable de ambiente
        run: echo "MI_VARIABLE="Hola Mundo, Jesús" >> $GITHUB_ENV

      - name: Ejecuta archivo de javascrip
        run: node main.js
